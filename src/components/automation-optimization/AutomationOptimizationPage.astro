---
import ServiceContactSection from '../contact/ServiceContactSection.astro';
import ServicePageAnimations from '../service-animation/ServicePageAnimations.astro';
import type { ContactCopy } from '../../data/contact';
import type { AutomationOptimizationContent } from '../../data/automationOptimization';

type Props = {
  content: AutomationOptimizationContent;
  contactCopy: ContactCopy;
};

const { content, contactCopy } = Astro.props;
---

<main class="auto-page">
  <section class="auto-hero" aria-labelledby="auto-hero-title">
    <video class="auto-hero-video" aria-hidden="true" autoplay muted loop playsinline preload="metadata">
      <source src="/assets/binary-wave-bg.webm" type="video/webm" />
      <source src="/assets/binary-wave-bg.mp4" type="video/mp4" />
    </video>

    <div class="auto-hero-content">
      <p class="auto-eyebrow">{content.hero.eyebrow}</p>
      <h1 id="auto-hero-title">{content.hero.title}</h1>
      <p>{content.hero.copy}</p>
      <div class="auto-actions">
        <button type="button" data-contact-trigger>{content.hero.primaryCta}</button>
      </div>
    </div>
  </section>

  <section class="auto-method" aria-labelledby="auto-method-title">
    <div class="auto-section-heading">
      <p class="auto-eyebrow">{content.challenges.eyebrow}</p>
      <h2 id="auto-method-title">{content.challenges.title}</h2>
      <p>{content.challenges.copy}</p>
    </div>

    <div class="auto-method-map" data-no-reveal>
      <div class="auto-method-path" aria-hidden="true">
        <span></span>
        <span></span>
      </div>
      {content.challenges.items.map((item, index) => (
        <article class={`auto-method-step auto-method-step-${index + 1}`}>
          <span class="auto-step-index">{String(index + 1).padStart(2, '0')}</span>
          <div class="auto-step-node" aria-hidden="true">
            <span></span>
          </div>
          <p class="auto-step-label">{item.label}</p>
          <h3>{item.title}</h3>
          <p>{item.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <section id="servicios-incluidos" class="auto-services" aria-labelledby="auto-services-title">
    <div class="auto-section-heading">
      <p class="auto-eyebrow">{content.services.eyebrow}</p>
      <h2 id="auto-services-title">{content.services.title}</h2>
      <p>{content.services.copy}</p>
    </div>

    <div class="auto-service-grid">
      {content.services.items.map((item) => (
        <article class="auto-service-card" tabindex="0" aria-label={item.title}>
          <div class="auto-service-card-inner">
            <div class="auto-service-card-front">
              <span class="auto-service-code">{item.code}</span>
              <h3>{item.title}</h3>
              <p>{item.copy}</p>
            </div>
            <div class="auto-service-card-back" aria-hidden="true">
              <p class="auto-service-back-label">{content.services.activitiesLabel}</p>
              <ul class="auto-service-activities">
                {item.activities.map((activity) => <li>{activity}</li>)}
              </ul>
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>

  <section class="auto-benefits" aria-labelledby="auto-benefits-title">
    <div class="auto-section-heading">
      <p class="auto-eyebrow">{content.benefits.eyebrow}</p>
      <h2 id="auto-benefits-title">{content.benefits.title}</h2>
      <p>{content.benefits.copy}</p>
    </div>

    <div class="auto-benefit-grid">
      {content.benefits.items.map((benefit) => (
        <article class="auto-benefit-card">
          <span>{benefit.value}</span>
          <h3>{benefit.title}</h3>
          <p>{benefit.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="auto-approach" aria-labelledby="auto-approach-title">
    <div class="auto-section-heading">
      <p class="auto-eyebrow">{content.approach.eyebrow}</p>
      <h2 id="auto-approach-title">{content.approach.title}</h2>
      <p>{content.approach.copy}</p>
    </div>

    <div class="auto-approach-map">
      <div class="auto-approach-path" aria-hidden="true"></div>
      {content.approach.steps.map((step, index) => (
        <article class={`auto-approach-step auto-approach-step-${index + 1}`}>
          <span class="auto-step-index">{step.label}</span>
          <div class="auto-step-node" aria-hidden="true">
            <span></span>
          </div>
          <h3>{step.title}</h3>
          <p>{step.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <ServiceContactSection
    id="contacto"
    eyebrow={content.cta.eyebrow}
    title={content.cta.title}
    copy={content.cta.copy}
    contactCopy={contactCopy}
  />

  <ServicePageAnimations />
</main>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const getAutoNavOffset = () => {
    const nav = document.querySelector('.site-nav, .site-header, header');
    return nav instanceof HTMLElement ? Math.round(nav.getBoundingClientRect().height) : 0;
  };

  const getAutoViewportHeight = () => {
    const stableVh = Number.parseFloat(
      getComputedStyle(document.documentElement).getPropertyValue('--app-stable-vh'),
    );
    return Number.isFinite(stableVh) && stableVh > 0 ? stableVh : window.innerHeight;
  };

  const setupAutoMethodScroll = () => {
    const root = document.querySelector('.auto-page');
    if (!root || root.hasAttribute('data-auto-method-ready')) return;

    const method = root.querySelector('.auto-method');
    const steps = gsap.utils.toArray<HTMLElement>('.auto-method-step', method);

    if (!(method instanceof HTMLElement) || steps.length === 0) return;

    root.setAttribute('data-auto-method-ready', 'true');

    const matchMedia = gsap.matchMedia();
    const getSequenceEnd = (multiplier: number) =>
      `+=${Math.round(getAutoViewportHeight() * multiplier)}`;
    const getPinStart = () => `top top+=${getAutoNavOffset()}`;

    matchMedia.add('(prefers-reduced-motion: no-preference) and (min-width: 680px)', () => {
      method.classList.add('is-scroll-sequence');
      gsap.set(steps, { autoAlpha: 0, y: 56 });

      const timeline = gsap.timeline({
        defaults: { ease: 'none' },
        scrollTrigger: {
          id: 'auto-method-sequence-desktop',
          trigger: method,
          start: getPinStart,
          end: () => getSequenceEnd(steps.length * 0.72),
          pin: true,
          pinSpacing: true,
          scrub: 0.65,
          anticipatePin: 1,
          invalidateOnRefresh: true,
        },
      });

      steps.forEach((step) => {
        timeline
          .to(step, { autoAlpha: 1, y: 0, duration: 1, ease: 'power2.out' })
          .to({}, { duration: 0.22 });
      });

      return () => {
        timeline.kill();
        method.classList.remove('is-scroll-sequence');
        gsap.set(steps, { clearProps: 'all' });
      };
    });

    matchMedia.add('(prefers-reduced-motion: no-preference) and (max-width: 679px)', () => {
      method.classList.add('is-scroll-sequence', 'is-mobile-sequence');
      gsap.set(steps, { autoAlpha: 0, y: 46 });

      const timeline = gsap.timeline({
        defaults: { ease: 'none' },
        scrollTrigger: {
          id: 'auto-method-sequence-mobile',
          trigger: method,
          start: getPinStart,
          end: () => getSequenceEnd(steps.length * 0.86),
          pin: true,
          pinSpacing: true,
          scrub: 0.7,
          anticipatePin: 1,
          invalidateOnRefresh: true,
        },
      });

      timeline
        .to(steps[0], { autoAlpha: 1, y: 0, duration: 0.78, ease: 'power2.out' })
        .to({}, { duration: 0.34 });

      steps.slice(1).forEach((step, index) => {
        const previousStep = steps[index];
        const isLastStep = index === steps.length - 2;
        const label = `auto-step-${index + 2}`;

        timeline
          .add(label)
          .to(previousStep, { autoAlpha: 0, y: -22, duration: 0.5, ease: 'power1.in' }, label)
          .fromTo(
            step,
            { autoAlpha: 0, y: 46 },
            { autoAlpha: 1, y: 0, duration: 0.78, ease: 'power2.out' },
            label,
          )
          .to({}, { duration: isLastStep ? 0.62 : 0.32 });
      });

      return () => {
        timeline.kill();
        method.classList.remove('is-scroll-sequence', 'is-mobile-sequence');
        gsap.set(steps, { clearProps: 'all' });
      };
    });

    window.addEventListener('agsit:viewport-change', () => ScrollTrigger.refresh());
  };

  const setupAutoFlipCards = () => {
    const cards = document.querySelectorAll<HTMLElement>('.auto-service-card');
    if (cards.length === 0) return;
    const isTouch = window.matchMedia('(hover: none)').matches;
    cards.forEach((card) => {
      if (isTouch) {
        card.addEventListener('click', () => card.classList.toggle('is-flipped'));
      }
      card.addEventListener('keydown', (e: KeyboardEvent) => {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          card.classList.toggle('is-flipped');
        }
      });
    });
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
      setupAutoFlipCards();
      setupAutoMethodScroll();
    }, { once: true });
  } else {
    setupAutoFlipCards();
    setupAutoMethodScroll();
  }
</script>
