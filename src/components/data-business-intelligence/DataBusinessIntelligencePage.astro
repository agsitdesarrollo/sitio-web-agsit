---
import ServiceContactSection from '../contact/ServiceContactSection.astro';
import ServicePageAnimations from '../service-animation/ServicePageAnimations.astro';
import type { ContactCopy } from '../../data/contact';
import type { DataBusinessIntelligenceContent } from '../../data/dataBusinessIntelligence';

type Props = {
  content: DataBusinessIntelligenceContent;
  contactCopy: ContactCopy;
};

const { content, contactCopy } = Astro.props;
---

<main class="data-page">
  <section class="data-hero" aria-labelledby="data-hero-title">
    <video class="data-hero-video" aria-hidden="true" autoplay muted loop playsinline preload="metadata">
      <source src="/assets/binary-wave-bg.webm" type="video/webm" />
      <source src="/assets/binary-wave-bg.mp4" type="video/mp4" />
    </video>

    <div class="data-hero-content">
      <p class="data-eyebrow">{content.hero.eyebrow}</p>
      <h1 id="data-hero-title">{content.hero.title}</h1>
      <p>{content.hero.copy}</p>
      <div class="data-actions">
        <button type="button" data-contact-trigger>{content.hero.primaryCta}</button>
      </div>
    </div>
  </section>

  <section class="data-method" aria-labelledby="data-method-title">
    <div class="data-section-heading">
      <p class="data-eyebrow">{content.challenges.eyebrow}</p>
      <h2 id="data-method-title">{content.challenges.title}</h2>
      <p>{content.challenges.copy}</p>
    </div>

    <div class="data-method-map" data-no-reveal>
      <div class="data-method-path" aria-hidden="true">
        <span></span>
        <span></span>
      </div>
      {content.challenges.items.map((item, index) => (
        <article class={`data-method-step data-method-step-${index + 1}`}>
          <span class="data-step-index">{String(index + 1).padStart(2, '0')}</span>
          <div class="data-step-node" aria-hidden="true">
            <span></span>
          </div>
          <p class="data-step-label">{item.label}</p>
          <h3>{item.title}</h3>
          <p>{item.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <section id="servicios-incluidos" class="data-services" aria-labelledby="data-services-title">
    <div class="data-section-heading">
      <p class="data-eyebrow">{content.services.eyebrow}</p>
      <h2 id="data-services-title">{content.services.title}</h2>
      <p>{content.services.copy}</p>
    </div>

    <div class="data-service-grid" data-no-reveal>
      {content.services.items.map((item) => (
        <article class="data-service-card" tabindex="0" aria-label={item.title}>
          <div class="data-service-card-inner">
            <div class="data-service-card-front">
              <span class="data-service-code">{item.code}</span>
              <h3>{item.title}</h3>
              <p>{item.copy}</p>
            </div>
            <div class="data-service-card-back" aria-hidden="true">
              <p class="data-service-back-label">{content.services.activitiesLabel}</p>
              <ul class="data-service-activities">
                {item.activities.map((activity) => <li>{activity}</li>)}
              </ul>
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>

  <section class="data-benefits" aria-labelledby="data-benefits-title">
    <div class="data-section-heading">
      <p class="data-eyebrow">{content.benefits.eyebrow}</p>
      <h2 id="data-benefits-title">{content.benefits.title}</h2>
      <p>{content.benefits.copy}</p>
    </div>

    <div class="data-benefit-grid">
      {content.benefits.items.map((benefit) => (
        <article class="data-benefit-card">
          <span>{benefit.value}</span>
          <h3>{benefit.title}</h3>
          <p>{benefit.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="data-approach" aria-labelledby="data-approach-title">
    <div class="data-section-heading">
      <p class="data-eyebrow">{content.approach.eyebrow}</p>
      <h2 id="data-approach-title">{content.approach.title}</h2>
      <p>{content.approach.copy}</p>
    </div>

    <div class="data-approach-map">
      <div class="data-approach-path" aria-hidden="true"></div>
      {content.approach.steps.map((step, index) => (
        <article class={`data-approach-step data-approach-step-${index + 1}`}>
          <span class="data-step-index">{step.label}</span>
          <div class="data-step-node" aria-hidden="true">
            <span></span>
          </div>
          <h3>{step.title}</h3>
          <p>{step.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <ServiceContactSection
    id="contacto"
    eyebrow={content.cta.eyebrow}
    title={content.cta.title}
    copy={content.cta.copy}
    contactCopy={contactCopy}
  />

  <ServicePageAnimations />
</main>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const getDataNavOffset = () => {
    const nav = document.querySelector('.site-nav, .site-header, header');
    return nav instanceof HTMLElement ? Math.round(nav.getBoundingClientRect().height) : 0;
  };

  const getDataViewportHeight = () => {
    const stableVh = Number.parseFloat(
      getComputedStyle(document.documentElement).getPropertyValue('--app-stable-vh'),
    );
    return Number.isFinite(stableVh) && stableVh > 0 ? stableVh : window.innerHeight;
  };

  const setupDataMethodScroll = () => {
    const root = document.querySelector('.data-page');
    if (!root || root.hasAttribute('data-data-method-ready')) return;

    const method = root.querySelector('.data-method');
    const steps = gsap.utils.toArray<HTMLElement>('.data-method-step', method);

    if (!(method instanceof HTMLElement) || steps.length === 0) return;

    root.setAttribute('data-data-method-ready', 'true');

    const matchMedia = gsap.matchMedia();
    const getSequenceEnd = (multiplier: number) =>
      `+=${Math.round(getDataViewportHeight() * multiplier)}`;
    const getPinStart = () => `top top+=${getDataNavOffset()}`;

    matchMedia.add('(prefers-reduced-motion: no-preference) and (min-width: 680px)', () => {
      method.classList.add('is-scroll-sequence');
      gsap.set(steps, { autoAlpha: 0, y: 56 });

      const timeline = gsap.timeline({
        defaults: { ease: 'none' },
        scrollTrigger: {
          id: 'data-method-sequence-desktop',
          trigger: method,
          start: getPinStart,
          end: () => getSequenceEnd(steps.length * 0.72),
          pin: true,
          pinSpacing: true,
          scrub: 0.65,
          anticipatePin: 1,
          invalidateOnRefresh: true,
        },
      });

      steps.forEach((step) => {
        timeline
          .to(step, { autoAlpha: 1, y: 0, duration: 1, ease: 'power2.out' })
          .to({}, { duration: 0.22 });
      });

      return () => {
        timeline.kill();
        method.classList.remove('is-scroll-sequence');
        gsap.set(steps, { clearProps: 'all' });
      };
    });

    matchMedia.add('(prefers-reduced-motion: no-preference) and (max-width: 679px)', () => {
      method.classList.add('is-scroll-sequence', 'is-mobile-sequence');
      gsap.set(steps, { autoAlpha: 0, y: 46 });

      const timeline = gsap.timeline({
        defaults: { ease: 'none' },
        scrollTrigger: {
          id: 'data-method-sequence-mobile',
          trigger: method,
          start: getPinStart,
          end: () => getSequenceEnd(steps.length * 0.86),
          pin: true,
          pinSpacing: true,
          scrub: 0.7,
          anticipatePin: 1,
          invalidateOnRefresh: true,
        },
      });

      timeline
        .to(steps[0], { autoAlpha: 1, y: 0, duration: 0.78, ease: 'power2.out' })
        .to({}, { duration: 0.34 });

      steps.slice(1).forEach((step, index) => {
        const previousStep = steps[index];
        const isLastStep = index === steps.length - 2;
        const label = `data-step-${index + 2}`;

        timeline
          .add(label)
          .to(previousStep, { autoAlpha: 0, y: -22, duration: 0.5, ease: 'power1.in' }, label)
          .fromTo(
            step,
            { autoAlpha: 0, y: 46 },
            { autoAlpha: 1, y: 0, duration: 0.78, ease: 'power2.out' },
            label,
          )
          .to({}, { duration: isLastStep ? 0.62 : 0.32 });
      });

      return () => {
        timeline.kill();
        method.classList.remove('is-scroll-sequence', 'is-mobile-sequence');
        gsap.set(steps, { clearProps: 'all' });
      };
    });

    window.addEventListener('agsit:viewport-change', () => ScrollTrigger.refresh());
  };

  const setupDataFlipCards = () => {
    const cards = document.querySelectorAll<HTMLElement>('.data-service-card');
    if (cards.length === 0) return;
    const isTouch = window.matchMedia('(hover: none)').matches;
    cards.forEach((card) => {
      if (isTouch) {
        card.addEventListener('click', () => card.classList.toggle('is-flipped'));
      }
      card.addEventListener('keydown', (e: KeyboardEvent) => {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          card.classList.toggle('is-flipped');
        }
      });
    });
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
      setupDataFlipCards();
      setupDataMethodScroll();
    }, { once: true });
  } else {
    setupDataFlipCards();
    setupDataMethodScroll();
  }
</script>
