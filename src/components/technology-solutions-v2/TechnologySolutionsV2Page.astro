---
import ContactDirectCard from '../contact/ContactDirectCard.astro';
import ContactForm from '../contact/ContactForm.astro';
import type { ContactCopy } from '../../data/contact';
import type { TechnologySolutionsV2Content } from '../../data/technologySolutionsV2';

type Props = {
  content: TechnologySolutionsV2Content;
  contactCopy: ContactCopy;
};

const { content, contactCopy } = Astro.props;
---

<main class="technology-v2-page">
  <section class="tech-v2-hero" aria-labelledby="tech-v2-hero-title">
    <div class="tech-v2-data-scene" aria-hidden="true">
      <span class="tech-v2-data-plane tech-v2-data-plane-a"></span>
      <span class="tech-v2-data-plane tech-v2-data-plane-b"></span>
      <span class="tech-v2-scan-line"></span>
    </div>

    <div class="tech-v2-hero-content">
      <p class="tech-v2-eyebrow">{content.hero.eyebrow}</p>
      <h1 id="tech-v2-hero-title">{content.hero.title}</h1>
      <p>{content.hero.copy}</p>
      <div class="tech-v2-actions">
        <button type="button" data-contact-trigger>{content.hero.primaryCta}</button>
        <a href="#capacidades">{content.hero.secondaryCta}</a>
      </div>
    </div>

  </section>

  <section class="tech-v2-method" aria-labelledby="tech-v2-method-title">
    <div class="tech-v2-section-heading">
      <p class="tech-v2-eyebrow">{content.method.eyebrow}</p>
      <h2 id="tech-v2-method-title">{content.method.title}</h2>
      <p>{content.method.copy}</p>
    </div>

    <div class="tech-v2-method-map" aria-label="Método de trabajo AGSIT">
      <div class="tech-v2-method-path" aria-hidden="true">
        <span></span>
        <span></span>
      </div>
      {
        content.method.steps.map((step, index) => (
          <article class={`tech-v2-method-step tech-v2-method-step-${index + 1}`}>
            <span class="tech-v2-step-index">{String(index + 1).padStart(2, '0')}</span>
            <div class="tech-v2-step-node" aria-hidden="true">
              <span></span>
            </div>
            <p class="tech-v2-step-label">{step.label}</p>
            <h3>{step.title}</h3>
            <p>{step.copy}</p>
            <strong>{step.outcome}</strong>
          </article>
        ))
      }
    </div>
  </section>

  <section id="capacidades" class="tech-v2-capabilities" aria-labelledby="tech-v2-capabilities-title">
    <div class="tech-v2-capabilities-intro">
      <p class="tech-v2-eyebrow">{content.capabilities.eyebrow}</p>
      <h2 id="tech-v2-capabilities-title">{content.capabilities.title}</h2>
      <p>{content.capabilities.copy}</p>
    </div>

    <div class="tech-v2-capability-grid">
      {
        content.capabilities.items.map((item) => (
          <article class="tech-v2-capability">
            <div>
              <span>{item.code}</span>
              <h3>{item.title}</h3>
              <p>{item.copy}</p>
            </div>
            <ul>
              {item.services.map((service) => <li>{service}</li>)}
            </ul>
          </article>
        ))
      }
    </div>
  </section>

  <section id="contacto" class="tech-v2-contact" aria-labelledby="tech-v2-contact-title">
    <div class="tech-v2-contact-heading">
      <p class="tech-v2-eyebrow">{content.contact.eyebrow}</p>
      <h2 id="tech-v2-contact-title">{content.contact.title}</h2>
      <p>{content.contact.copy}</p>
    </div>

    <div class="tech-v2-contact-grid">
      <ContactDirectCard copy={contactCopy.direct} />
      <div class="tech-v2-contact-form">
        <ContactForm copy={contactCopy.form} />
      </div>
    </div>
  </section>
</main>

<script>
  const setupTechnologyV2Reveal = () => {
    const root = document.querySelector('.technology-v2-page');

    if (!root || root.hasAttribute('data-tech-v2-ready')) return;

    root.setAttribute('data-tech-v2-ready', 'true');

    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const items = Array.from(
      root.querySelectorAll(
        '.tech-v2-hero-content, .tech-v2-section-heading, .tech-v2-method-step, .tech-v2-capabilities-intro, .tech-v2-capability, .tech-v2-contact-heading, .tech-v2-contact-grid',
      ),
    ).filter((item) => item instanceof HTMLElement);

    items.forEach((item, index) => {
      item.setAttribute('data-tech-v2-reveal', '');
      item.style.setProperty('--tech-v2-delay', `${Math.min(index % 7, 6) * 65}ms`);
    });

    if (reduceMotion || !('IntersectionObserver' in window)) {
      items.forEach((item) => item.classList.add('is-visible'));
      return;
    }

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;

          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        });
      },
      {
        rootMargin: '0px 0px -12% 0px',
        threshold: 0.12,
      },
    );

    items.forEach((item) => observer.observe(item));
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', setupTechnologyV2Reveal, { once: true });
  } else {
    setupTechnologyV2Reveal();
  }
</script>
