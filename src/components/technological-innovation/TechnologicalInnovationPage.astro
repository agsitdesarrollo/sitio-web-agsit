---
import ServiceContactSection from '../contact/ServiceContactSection.astro';
import ServicePageAnimations from '../service-animation/ServicePageAnimations.astro';
import type { ContactCopy } from '../../data/contact';
import type { TechnologicalInnovationContent } from '../../data/technologicalInnovation';

type Props = {
  content: TechnologicalInnovationContent;
  contactCopy: ContactCopy;
};

const { content, contactCopy } = Astro.props;
---

<main class="inn-page">
  <section class="inn-hero" aria-labelledby="inn-hero-title">
    <video class="inn-hero-video" aria-hidden="true" autoplay muted loop playsinline preload="metadata">
      <source src="/assets/binary-wave-bg.webm" type="video/webm" />
      <source src="/assets/binary-wave-bg.mp4" type="video/mp4" />
    </video>

    <div class="inn-hero-content">
      <p class="inn-eyebrow">{content.hero.eyebrow}</p>
      <h1 id="inn-hero-title">{content.hero.title}</h1>
      <p>{content.hero.copy}</p>
      <div class="inn-actions">
        <button type="button" data-contact-trigger>{content.hero.primaryCta}</button>
      </div>
    </div>
  </section>

  <section class="inn-method" aria-labelledby="inn-method-title">
    <div class="inn-section-heading">
      <p class="inn-eyebrow">{content.challenges.eyebrow}</p>
      <h2 id="inn-method-title">{content.challenges.title}</h2>
      <p>{content.challenges.copy}</p>
    </div>

    <div class="inn-method-map" data-no-reveal>
      <div class="inn-method-path" aria-hidden="true">
        <span></span>
        <span></span>
      </div>
      {content.challenges.items.map((item, index) => (
        <article class={`inn-method-step inn-method-step-${index + 1}`}>
          <span class="inn-step-index">{String(index + 1).padStart(2, '0')}</span>
          <div class="inn-step-node" aria-hidden="true">
            <span></span>
          </div>
          <p class="inn-step-label">{item.label}</p>
          <h3>{item.title}</h3>
          <p>{item.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <section id="servicios-incluidos" class="inn-services" aria-labelledby="inn-services-title">
    <div class="inn-section-heading">
      <p class="inn-eyebrow">{content.services.eyebrow}</p>
      <h2 id="inn-services-title">{content.services.title}</h2>
      <p>{content.services.copy}</p>
    </div>

    <div class="inn-service-grid" data-no-reveal>
      {content.services.items.map((item) => (
        <article class="inn-service-card" tabindex="0" aria-label={item.title}>
          <div class="inn-service-card-inner">
            <div class="inn-service-card-front">
              <span class="inn-service-code">{item.code}</span>
              <h3>{item.title}</h3>
              <p>{item.copy}</p>
            </div>
            <div class="inn-service-card-back" aria-hidden="true">
              <p class="inn-service-back-label">{content.services.activitiesLabel}</p>
              <ul class="inn-service-activities">
                {item.activities.map((activity) => <li>{activity}</li>)}
              </ul>
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>

  <section class="inn-benefits" aria-labelledby="inn-benefits-title">
    <div class="inn-section-heading">
      <p class="inn-eyebrow">{content.benefits.eyebrow}</p>
      <h2 id="inn-benefits-title">{content.benefits.title}</h2>
      <p>{content.benefits.copy}</p>
    </div>

    <div class="inn-benefit-grid">
      {content.benefits.items.map((benefit) => (
        <article class="inn-benefit-card">
          <span>{benefit.value}</span>
          <h3>{benefit.title}</h3>
          <p>{benefit.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="inn-approach" aria-labelledby="inn-approach-title">
    <div class="inn-section-heading">
      <p class="inn-eyebrow">{content.approach.eyebrow}</p>
      <h2 id="inn-approach-title">{content.approach.title}</h2>
      <p>{content.approach.copy}</p>
    </div>

    <div class="inn-approach-map">
      <div class="inn-approach-path" aria-hidden="true"></div>
      {content.approach.steps.map((step, index) => (
        <article class={`inn-approach-step inn-approach-step-${index + 1}`}>
          <span class="inn-step-index">{step.label}</span>
          <div class="inn-step-node" aria-hidden="true">
            <span></span>
          </div>
          <h3>{step.title}</h3>
          <p>{step.copy}</p>
        </article>
      ))}
    </div>
  </section>

  <ServiceContactSection
    id="contacto"
    eyebrow={content.cta.eyebrow}
    title={content.cta.title}
    copy={content.cta.copy}
    contactCopy={contactCopy}
  />

  <ServicePageAnimations />
</main>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const getInnNavOffset = () => {
    const nav = document.querySelector('.site-nav, .site-header, header');
    return nav instanceof HTMLElement ? Math.round(nav.getBoundingClientRect().height) : 0;
  };

  const getInnViewportHeight = () => {
    const stableVh = Number.parseFloat(
      getComputedStyle(document.documentElement).getPropertyValue('--app-stable-vh'),
    );
    return Number.isFinite(stableVh) && stableVh > 0 ? stableVh : window.innerHeight;
  };

  const setupInnMethodScroll = () => {
    const root = document.querySelector('.inn-page');
    if (!root || root.hasAttribute('data-inn-method-ready')) return;

    const method = root.querySelector('.inn-method');
    const steps = gsap.utils.toArray<HTMLElement>('.inn-method-step', method);

    if (!(method instanceof HTMLElement) || steps.length === 0) return;

    root.setAttribute('data-inn-method-ready', 'true');

    const matchMedia = gsap.matchMedia();
    const getSequenceEnd = (multiplier: number) =>
      `+=${Math.round(getInnViewportHeight() * multiplier)}`;
    const getPinStart = () => `top top+=${getInnNavOffset()}`;

    matchMedia.add('(prefers-reduced-motion: no-preference) and (min-width: 680px)', () => {
      method.classList.add('is-scroll-sequence');
      gsap.set(steps, { autoAlpha: 0, y: 56 });

      const timeline = gsap.timeline({
        defaults: { ease: 'none' },
        scrollTrigger: {
          id: 'inn-method-sequence-desktop',
          trigger: method,
          start: getPinStart,
          end: () => getSequenceEnd(steps.length * 0.72),
          pin: true,
          pinSpacing: true,
          scrub: 0.65,
          anticipatePin: 1,
          invalidateOnRefresh: true,
        },
      });

      steps.forEach((step) => {
        timeline
          .to(step, { autoAlpha: 1, y: 0, duration: 1, ease: 'power2.out' })
          .to({}, { duration: 0.22 });
      });

      return () => {
        timeline.kill();
        method.classList.remove('is-scroll-sequence');
        gsap.set(steps, { clearProps: 'all' });
      };
    });

    matchMedia.add('(prefers-reduced-motion: no-preference) and (max-width: 679px)', () => {
      method.classList.add('is-scroll-sequence', 'is-mobile-sequence');
      gsap.set(steps, { autoAlpha: 0, y: 46 });

      const timeline = gsap.timeline({
        defaults: { ease: 'none' },
        scrollTrigger: {
          id: 'inn-method-sequence-mobile',
          trigger: method,
          start: getPinStart,
          end: () => getSequenceEnd(steps.length * 0.86),
          pin: true,
          pinSpacing: true,
          scrub: 0.7,
          anticipatePin: 1,
          invalidateOnRefresh: true,
        },
      });

      timeline
        .to(steps[0], { autoAlpha: 1, y: 0, duration: 0.78, ease: 'power2.out' })
        .to({}, { duration: 0.34 });

      steps.slice(1).forEach((step, index) => {
        const previousStep = steps[index];
        const isLastStep = index === steps.length - 2;
        const label = `inn-step-${index + 2}`;

        timeline
          .add(label)
          .to(previousStep, { autoAlpha: 0, y: -22, duration: 0.5, ease: 'power1.in' }, label)
          .fromTo(
            step,
            { autoAlpha: 0, y: 46 },
            { autoAlpha: 1, y: 0, duration: 0.78, ease: 'power2.out' },
            label,
          )
          .to({}, { duration: isLastStep ? 0.62 : 0.32 });
      });

      return () => {
        timeline.kill();
        method.classList.remove('is-scroll-sequence', 'is-mobile-sequence');
        gsap.set(steps, { clearProps: 'all' });
      };
    });

    window.addEventListener('agsit:viewport-change', () => ScrollTrigger.refresh());
  };

  const setupInnFlipCards = () => {
    const cards = document.querySelectorAll<HTMLElement>('.inn-service-card');
    if (cards.length === 0) return;
    const isTouch = window.matchMedia('(hover: none)').matches;
    cards.forEach((card) => {
      if (isTouch) {
        card.addEventListener('click', () => card.classList.toggle('is-flipped'));
      }
      card.addEventListener('keydown', (e: KeyboardEvent) => {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          card.classList.toggle('is-flipped');
        }
      });
    });
  };

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', () => {
      setupInnFlipCards();
      setupInnMethodScroll();
    }, { once: true });
  } else {
    setupInnFlipCards();
    setupInnMethodScroll();
  }
</script>
